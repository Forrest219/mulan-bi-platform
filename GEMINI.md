# Codex MCP Configuration

## 快速开始

Codex CLI 已配置为项目默认 MCP server，可直接使用 `/codex` 命令进行代码分析和操作。

## 可用命令

- 使用 Claude Code CLI 直接对话
- 通过 Codex MCP server 进行代码分析

## 环境

开发环境配置：
- 后端：FastAPI + SQLAlchemy + PostgreSQL
- 前端：React 19 + TypeScript + Vite
- 数据库：PostgreSQL 16 (Docker)
- 缓存：Redis 7 (Docker)
