# Mock Regression Tests

Place mock-backed Playwright regression specs in this directory.

The `test:mock-regression` npm script is scoped to `playwright.mock-regression.config.ts`, whose `testDir` points here. This directory intentionally contains no placeholder passing spec.
