<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ notification.title }}</title>
  <style>
    body { font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif; max-width: 600px; margin: 0 auto; padding: 20px; color: #333; }
    .header { border-left: 4px solid #1890ff; padding-left: 12px; margin-bottom: 20px; }
    .title { font-size: 18px; font-weight: 600; margin: 0 0 8px 0; color: #1a1a1a; }
    .content { font-size: 14px; line-height: 1.6; color: #666; background: #f7f8fa; border-radius: 8px; padding: 16px; margin: 16px 0; }
    .event-type { font-size: 12px; color: #999; margin-top: 16px; }
    .footer { font-size: 12px; color: #999; margin-top: 24px; border-top: 1px solid #eee; padding-top: 12px; }
    .level-error { border-left-color: #ff4d4f; }
    .level-warning { border-left-color: #faad14; }
    .level-success { border-left-color: #52c41a; }
  </style>
</head>
<body>
  <div class="header level-{{ notification.level }}">
    <h1 class="title">{{ notification.title }}</h1>
  </div>
  <div class="content">{{ notification.content }}</div>
  {% if payload %}
  <div class="event-type">
    <strong>事件类型：</strong>{{ event_type }}<br>
    {% if event_id %}<strong>事件 ID：</strong>{{ event_id }}{% endif %}
  </div>
  {% endif %}
  <div class="footer">
    此邮件由木兰 BI 平台自动发出，请勿直接回复。<br>
    如有疑问，请联系系统管理员。
  </div>
</body>
</html>