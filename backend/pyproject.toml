[project]
name = "mulan-bi-backend"
version = "0.1.0"
description = "Mulan BI Platform Backend"
requires-python = ">=3.10"

[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["app*", "services*"]
